apiVersion: v1
clusters:
- cluster:
    certificate-authority-data: ${CA_DATA}
    server: ${SERVER_URL}
  name: ${NAME}
contexts:
- context:
    cluster: ${NAME}
    user: ${NAME}
  name: ${NAME}
current-context: ${NAME}
kind: Config
preferences: {}
users:
- name: ${NAME}
  user:
    token: ${TOKEN}
